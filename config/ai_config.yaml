# Card Detector Config

gemini:
  # Danh sách API keys (thêm bao nhiêu cũng được)
  api_keys:
    - "AIzaSyCNpni_0tKU84z2bYCV15dYu9NDF2N8Z2E"  # Key 1 - Gmail chính

# Cache configuration
cache:
  enabled: true
  dir: "data/cache/detections"
  max_age_hours: 168

# YOLO - KHÔNG DÙNG
yolo:
  enabled: false